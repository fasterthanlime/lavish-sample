
namespace sample {
    client fn reverse(s: string) -> (s: string)

    server fn print(s: string, reversed: bool)
    server fn show_stats()

    server fn greet(name: string, title: Option<string>)
    server fn get_cookies() -> (cookies: Map<string, string>)
    server fn reverse_list(input: Array<string>) -> (output: Array<string>)
}
